================================================================================
 docs/02_review_checklist.txt  —  レビュー観点（Copilot / Gemini / セルフ共通・全PR）
================================================================================

■ 全PR共通
  [ ] Zero-Dependency 違反なし（外部CSS/JS/PSモジュール/ランタイム/PIA を足していない）
  [ ] スコープ外のファイル・挙動を変更していない
  [ ] “触るな”リスト（00_ai_context.txt）の既存実装を退行させていない
  [ ] build.ps1 が通り、Parser::ParseFile がエラー0
  [ ] tests/run-tests.ps1 が全通過
  [ ] コミットが Conventional Commits / 論理単位で分割されている

■ PR#1（テスト基盤・characterization）
  [ ] src/ を1mmも変更していない（抽出・挙動変更をしていない）
  [ ] 層1テスト（AtEnd / Get-PptFiles / New-SecurePin）が 01_spec の期待表どおり
  [ ] 層2（cid/pin/route）は期待表を pending として保持し、1.3 結線を前提にしている
  [ ] テストが black-box（入力→出力）で、内部実装に依存していない
  [ ] CI に「パース検証」「テスト実行」が入り、失敗時に非0で落ちる
  [ ] Pester 等を製品依存に追加していない（if/throw もしくは CI限定モジュール）

■ PR#2（Phase 1.1・フロント分離 / -f 全廃）
  [ ] templates のヒアドキュメントが src/frontend/** に分離されている
  [ ] $script:HtmlTemplates.* に -f が一切残っていない（残骸ガードB が通る）
  [ ] dist に %%VERSION%%/%%ASSET_*%% 等のビルド時トークンが残っていない（残骸ガードA）
  [ ] %%DECK%% は実行時置換のまま（許可リスト唯一の例外）で、HtmlEncode が維持されている
  [ ] 置換順序「信頼トークン先・デッキ名最後」になっている
  [ ] PIN 等の秘密を Web 配信テンプレートのトークンにしていない
  [ ] hold 系 JS が hold.js に一本化され、重複が解消されている
  [ ] version が単一ソースから注入され、コンソール表示と一致している

■ Pages モックアップに影響する変更（PR-L / PR-M 以降・該当PRのみ）
  [ ] src/frontend の <head> 構造を変えた場合、build-mockup.ps1 の注入ガード
      （'</head>' がちょうど1個）が通る（壊れていれば生成が exit 1 で落ちる）
  [ ] 製品側の HTTP 契約を変えた場合、build/mockup/demo-shim.js の疑似バックエンドが
      追従している。対象は route の追加/改名・/status が返す文字列・
      /slide/state の JSON 形状・frontend の fetch 先。
      ※ 追従漏れは CI では検出できず、Pages のモックアップだけが静かに壊れる
  [ ] サンプルデッキ名・デモPIN を変えた場合、build-mockup.ps1 側の単一定義
      （$activeDecks / $finishedDecks / $demoPin）だけを直している
      （shim 側に値をハードコードして二重管理に戻していない）
  [ ] モックアップ生成物（dist/pages/）を git にコミットしていない
  [ ] CI 限定ツールのままで、dist/presentation-controller.ps1 に同梱していない

■ 将来検討（このロードマップでは“変えない”が、記録として残す候補）
  - PIN 正規表現のアンカー無し緩さ（'pin=1234567'→'123456' / 'xpin='許容）。
    厳格化（^pin=([0-9]{6})$ 等）は別PRの意図的判断として扱う。
  - 認証の失敗ロックアウト未実装（IP単位1秒スロットルのみ）。
