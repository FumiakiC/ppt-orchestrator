# docs/design/v2 — v2 UI リデザイン "Quiet Signal" 設計資料

Kimi3 が作成した UI/UX リデザイン・ハンドオフパッケージ
（`Kimi_Agent_远程控制UX优化.zip` 内 `ppt-orchestrator_uiux_handoff/`、2026-07-29 再検証版）からの取り込み。
実装キャンペーンの計画・PR 分割・確定済み仕様判断は **`docs/07_v2_ui_redesign_plan.md`** を参照。

## 内容

| ファイル | 役割 |
|---|---|
| `02_原版リポジトリ調査メモ.md` | 取り込み時点（main @ `440e11d`）の現行コード調査結果。§10 に 2026-07-29 再検証の訂正あり |
| `03_UIUXデザイン仕様書.md` | リデザインの完全仕様（トークン / 画面 / 挙動 / レスポンシブ） |
| `04_JS構造継承プラン.md` | **フック契約の正本**。無改変継承範囲・DOM フック表・新旧ネーミング対応・CSS 変数マッピング |
| `reference/mockup/` | 動作する参照モックアップ（ビジュアルの正本）。`mockup.html` をブラウザで直接開ける（画像非依存） |

## 取り込み時の注意

- 資料は**ハンドオフ原文のまま**（verbatim）。原文と docs/07 の確定判断が食い違う箇所は **docs/07 が優先**する
  （例: フォントは現行スタック維持で Inter / JetBrains Mono 名は導入しない、キーヒントバーは SPACE 表記なし、
  Auth エラーは変種ページ方式維持）。
- パッケージ同梱のスクリーンショット 13 点は**取り込んでいない**（モックアップが画像非依存で動作するため。
  必要時は元 zip を参照）。`03` §8 のスクリーンショット言及は元パッケージ内の相対参照であり本ディレクトリには存在しない。
- `reference/mockup/` は**製品コードではない**（`dist/` 非同梱・ビルド非接触の設計資料）。Zero-Dependency 制約の対象外。
